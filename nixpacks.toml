[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["rm -rf dist node_modules/.vite 2>/dev/null || true", "npm run build"]

[start]
cmd = "npm run start:railway"
